# Environment variables are now scoped per app:
#   apps/api/.env         — API server config
#   apps/web/.env          — Web app config
#   packages/database/.env — Prisma migrations
#
# Copy the .env.example from each directory and fill in your values.
